# Runtime dependencies for Streamlit Community Cloud deployment.
# (Local dev uses uv + pyproject.toml; this mirrors the runtime deps.)
crewai==1.11.0
litellm>=1.82.5
streamlit>=1.55.0
pypdf>=4.0
altair>=5.0
pandas>=2.0
